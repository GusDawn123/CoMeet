{
  "name": "comeet",
  "version": "1.0.0",
  "private": true,
  "description": "AI Meeting Assistant — Real-time interview copilot with multi-provider LLM streaming",
  "workspaces": [
    "packages/shared",
    "packages/ai-engine",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev -w packages/server",
    "dev:client": "npm run dev -w packages/client",
    "build": "npm run build -w packages/shared && npm run build -w packages/ai-engine && npm run build -w packages/server && npm run build -w packages/client",
    "db:migrate": "npm run db:migrate -w packages/server",
    "db:studio": "npm run db:studio -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.2"
  }
}
